package com.jits.library;

public class ShippingTypeValidator implements Comparator<T> {
		@Override
		public int compare(Parcel first, Parcel second){
			
			int rtn = -99;
			
			if(first.getId() == second.getId()) {
				rtn = 0;
				
			} else if(first.getId() > second.getId()){
				rtn = 1;
				
			} else {
				rtn = -1;
			}
			
			return rtn;
		}

}
